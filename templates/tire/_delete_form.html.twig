<form method="post" action="{{ path('app_tire_delete', {'id': tire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tire.id) }}">
    <button class="btn">Delete</button>
</form>
